# Render.com Deployment Guide - مجاني 100%

## لماذا Render؟

✅ **مجاني تماماً** - لا يحتاج بطاقة ائتمان  
✅ **PostgreSQL مجاني**  
✅ **HTTPS تلقائي**  
✅ **سهل الاستخدام**  
⚠️ **ملاحظة:** الخدمة تتوقف بعد 15 دقيقة من عدم النشاط (تعود للعمل خلال ثوانٍ عند الطلب)

---

## الخطوة 1️⃣: إنشاء حساب

1. اذهب إلى: https://render.com
2. انقر **"Get Started for Free"**
3. سجل باستخدام:
   - GitHub (موصى به)
   - أو GitLab
   - أو البريد الإلكتروني

---

## الخطوة 2️⃣: إنشاء PostgreSQL Database

### في لوحة التحكم:

1. انقر **"New +"**
2. اختر **"PostgreSQL"**
3. املأ المعلومات:
   - **Name:** `lectora-db`
   - **Database:** `lectora`
   - **User:** `lectora`
   - **Region:** اختر الأقرب لك
   - **Plan:** **Free** ✅
4. انقر **"Create Database"**
5. **انتظر 1-2 دقيقة** حتى يصبح جاهزاً
6. **انسخ "Internal Database URL"** (سنحتاجه لاحقاً)

---

## الخطوة 3️⃣: نشر الخادم

### الطريقة أ: من GitHub (الأسهل)

إذا كان الكود على GitHub:

1. انقر **"New +"** → **"Web Service"**
2. اختر **"Build and deploy from a Git repository"**
3. **Connect Repository:**
   - اربط حساب GitHub
   - اختر المستودع
   - اختر فرع `main` أو `master`
4. **Configure:**
   - **Name:** `lectora-server`
   - **Region:** نفس منطقة قاعدة البيانات
   - **Branch:** `main`
   - **Root Directory:** `server` (إذا كان الخادم في مجلد فرعي)
   - **Runtime:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `node index.js`
5. **Plan:** اختر **Free** ✅
6. انقر **"Create Web Service"**

### الطريقة ب: بدون GitHub (رفع مباشر)

إذا لم يكن الكود على GitHub:

#### 1. تثبيت Render CLI

```powershell
npm install -g @render/cli
```

#### 2. تسجيل الدخول

```powershell
render login
```

#### 3. النشر

```powershell
cd c:\Users\moham\.gemini\antigravity\scratch\college_app\server
render deploy
```

---

## الخطوة 4️⃣: تكوين متغيرات البيئة

### في صفحة Web Service:

1. اذهب إلى تبويب **"Environment"**
2. أضف المتغيرات التالية:

```
NODE_ENV=production
PORT=10000
DATABASE_URL=<الصق Internal Database URL من الخطوة 2>
JWT_SECRET=<سيتم توليده>
ALLOWED_ORIGINS=*
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads
```

### توليد JWT_SECRET

في PowerShell:
```powershell
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

انسخ الناتج واستخدمه كقيمة لـ `JWT_SECRET`

### حفظ المتغيرات

انقر **"Save Changes"** - سيعيد النشر تلقائياً

---

## الخطوة 5️⃣: الحصول على رابط الخادم

بعد اكتمال النشر:

1. في صفحة Web Service
2. ستجد الرابط في الأعلى:
   ```
   https://lectora-server.onrender.com
   ```
3. **انسخ هذا الرابط** ✅

---

## الخطوة 6️⃣: اختبار الخادم

### Health Check

افتح المتصفح:
```
https://lectora-server.onrender.com/api/health
```

يجب أن ترى:
```json
{
  "success": true,
  "message": "Server is running",
  "timestamp": "..."
}
```

### اختبار تسجيل الدخول

في PowerShell:
```powershell
$url = "https://lectora-server.onrender.com"
$body = @{
    identifier = "rep@college.edu"
    password = "admin"
    role = "representative"
} | ConvertTo-Json

Invoke-RestMethod -Uri "$url/api/login" -Method POST -Body $body -ContentType "application/json"
```

---

## الخطوة 7️⃣: تحديث التطبيق

### تحديث socket.js

افتح:
```
mobile-client/src/utils/socket.js
```

**غيّر السطر 5:**
```javascript
// قبل
const DEFAULT_SERVER_URL = 'http://172.20.10.2:3000';

// بعد (استخدم رابطك من Render)
const DEFAULT_SERVER_URL = 'https://lectora-server.onrender.com';
```

**احفظ الملف** ✅

---

## الخطوة 8️⃣: اختبار التطبيق

```powershell
cd mobile-client
npm start
```

اختبر جميع الميزات:
- ✅ تسجيل الدخول
- ✅ الدردشة
- ✅ رفع الملفات
- ✅ الإشعارات

---

## 🎉 تم النشر بنجاح!

الخادم الآن يعمل على الإنترنت مجاناً! 🚀

---

## ⚠️ ملاحظات مهمة

### الخدمة المجانية

> [!WARNING]
> **التوقف التلقائي:**
> - الخدمة تتوقف بعد 15 دقيقة من عدم النشاط
> - تعود للعمل خلال 30-60 ثانية عند أول طلب
> - هذا طبيعي في الخطة المجانية

**الحل:**
- استخدم خدمة مثل UptimeRobot لإبقاء الخادم نشطاً
- أو ترقية للخطة المدفوعة ($7/شهر)

### قاعدة البيانات

> [!IMPORTANT]
> الكود الحالي يستخدم SQLite، لكن Render يحتاج PostgreSQL.
> 
> **يجب تحديث الكود!** أخبرني لأساعدك في هذا.

### رفع الملفات

> [!TIP]
> - Render filesystem مؤقت
> - استخدم Cloudinary أو AWS S3 للملفات الدائمة

---

## 💰 التكلفة

| العنصر | التكلفة |
|--------|---------|
| Web Service | **مجاني** ✅ |
| PostgreSQL | **مجاني** ✅ |
| **الإجمالي** | **$0/شهر** 🎉 |

---

## 🔧 استكشاف الأخطاء

### البناء فشل
- تحقق من Logs في لوحة التحكم
- تأكد من `package.json` صحيح
- تحقق من جميع التبعيات موجودة

### خطأ قاعدة البيانات
- تأكد من نسخ `DATABASE_URL` بشكل صحيح
- تحقق من أن قاعدة البيانات جاهزة (Active)
- قد تحتاج تحديث الكود لـ PostgreSQL

### الخدمة بطيئة
- هذا طبيعي بعد التوقف (15 دقيقة)
- الطلب الأول يأخذ 30-60 ثانية
- الطلبات التالية سريعة

---

## 📊 مقارنة الخطط

| الميزة | Free | Starter ($7/شهر) |
|--------|------|------------------|
| التوقف التلقائي | نعم (15 دقيقة) | لا ❌ |
| PostgreSQL | ✅ | ✅ |
| HTTPS | ✅ | ✅ |
| Build Minutes | 500/شهر | Unlimited |

---

## الخطوات التالية

1. ✅ **نسخ رابط Render**
2. ✅ **تحديث `socket.js`**
3. ✅ **اختبار التطبيق**
4. ⚠️ **تحديث الكود لـ PostgreSQL** (أخبرني!)
5. ✅ **بناء التطبيق بـ EAS**
6. ✅ **النشر على المتاجر**

---

## 🆘 تحتاج مساعدة؟

- **Render Docs:** https://render.com/docs
- **Render Community:** https://community.render.com
- **أو اسألني!** 😊

---

## ✅ قائمة التحقق

- [ ] إنشاء حساب Render
- [ ] إنشاء PostgreSQL Database
- [ ] نشر Web Service
- [ ] تكوين متغيرات البيئة
- [ ] الحصول على الرابط
- [ ] اختبار الخادم
- [ ] تحديث التطبيق
- [ ] اختبار التطبيق الكامل

**جاهز؟ ابدأ الآن! 🚀**
